pair
```java
Queue<int[]> queue = new LinkedList<>();
queue.offer(new int[]{i , j}); //q.push({i,j});

!queue.isEmpty()  //q.empty()

int size = queue.size(); //q.size()

int[] point = queue.poll(); //q.pop();
//point[0] and point[1]
```